%%% Gradient Drift {layout=full-image transition=zoom background=gradient(#112233, #445566, 45)}
# Gradient drift

%%% Image Drift {layout=full-image transition=cube background=image(https://example.com/hero.png, 0.35)}
# Image drift
